# Context

## Current State
- Jekyll site runs locally at http://127.0.0.1:4000/.
- Local setup uses Homebrew `ruby@3.4`, Bundler `2.6.8`, Node, and ImageMagick.
- Ruby gems are installed in `vendor/bundle`.

## Next Step
- Make site content or frontend changes.
- Ask Codex to restart the local server when needed.

## Notes
- Repo uses the remote theme `daviddarnes/alembic`.
- `node_modules` is required for `lightgallery` assets.
- Local startup shows Sass and pagination warnings.
